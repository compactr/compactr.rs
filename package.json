{
  "name": "compactr-rs-testing",
  "version": "0.1.0",
  "description": "JavaScript dependencies for cross-compatibility testing with compactr.rs",
  "private": true,
  "scripts": {
    "generate-fixtures": "node compactr/tests/fixtures/generate_fixtures.js",
    "test": "npm run generate-fixtures && cargo test"
  },
  "dependencies": {
    "compactr": "^2.4.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
